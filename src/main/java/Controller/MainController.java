package Controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.*;

/**
 * @author chrisneven
 * The MainController class is responsible for handeling requests by using the DAO's for saving and the model for defining objects that are received.
 * from the frontend. The Controller is the bridge between mobile application and the DAO - database.
 */
@Controller
@RequestMapping(path="/")
public class MainController {

}
